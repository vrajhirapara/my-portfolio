# Web3Forms: https://web3forms.com — register with the inbox where you want contact form mail.
# Copy this file to .env.local (next to package.json), paste your access key, then fully restart the dev server (stop npm start, start again). CRA only reads env on startup.
REACT_APP_WEB3FORMS_ACCESS_KEY=
