# Web3Forms: https://web3forms.com — optional overrides (defaults live in src/config/web3forms.json).
#
# Copy to .env.local and set to override the bundled key (e.g. staging). Restart the dev server after changes.
# Legacy name: REACT_APP_WEB3FORMS_ACCESS_KEY
VITE_WEB3FORMS_ACCESS_KEY=
